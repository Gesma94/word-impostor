{
  "name": "word-impostor-shared",
  "version": "0.0.1",
  "description": "Shared for Word Impostor",
  "scripts": {
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
    "build:prod": "npm run clean && tsc"
  },
  "author": "Matteo Gesmundo",
  "license": "MIT",
  "devDependencies": {
    "rimraf": "^5.0.5"
  },
  "dependencies": {
    "@types/node": "^20.11.29"
  }
}
